# [라즈베리파이 5 / Debian 12 Bookworm & Debian 13 Trixie 전용 설정 가이드]
# gpiozero 및 lgpio는 라즈베리파이 OS 기본 APT 패키지로 제공되므로,
# C 빌드 오류를 방지하기 위해 가상환경 생성 시 --system-site-packages 옵션을 사용합니다:
#
# 1) 시스템 패키지 확인 및 설치:
#    sudo apt update && sudo apt install -y python3-gpiozero python3-lgpio
#
# 2) 가상환경 생성 (시스템 패키지 포함):
#    python3 -m venv --system-site-packages venv
#    source venv/bin/activate
#
# 3) 가상환경 내부에서 실행할 의존성:
requests>=2.31.0
python-dotenv>=1.0.1
