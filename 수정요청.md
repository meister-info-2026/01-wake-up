# 스타터 킷 및 플러그인 개선 요청 사항 (수정요청.md)

IoT·영상인식 스마트 제어 시스템 프로젝트의 초기 스캐폴딩부터 Mock 백엔드, DB, Next.js 대시보드, YOLOv8 영상인식 및 트리거 엔진 구현 과정에서 발생한 실제 **시행착오와 개선 요청 사항**을 정리한 문서입니다.

---

## 1. `frontend/` 스캐폴딩 시 `.env.example` 충돌 문제

### 🔴 문제 현상
- 저장소 템플릿에 `frontend/.env.example` 파일이 미리 존재할 경우, `create-next-app` 실행 시 디렉토리가 비어있지 않다는 충돌 에러가 발생하여 생성이 중단됨.
  ```
  The directory frontend contains files that could conflict: .env.example
  ```

### 💡 개선 요청
1. **스캐폴딩 순서 조정**: 학생용 매뉴얼에서 `create-next-app`을 먼저 실행한 후 `.env.example`을 생성하도록 가이드 수정, 또는
2. **사전 템플릿 제외**: 스타터 킷 배포 시 `frontend/.env.example`을 사전에 두지 않고 스캐폴드 완료 후 생성하도록 자동화 스크립트 보완.

---

## 2. `cd backend` 실행 시 `ModuleNotFoundError: No module named 'backend'`

### 🔴 문제 현상
- 학생용 매뉴얼 가이드대로 `backend` 폴더 내부로 이동하여 `uvicorn main:app --reload --port 8000`을 실행할 때, 현재 작업 디렉토리가 `backend/`이므로 `sys.path`에 상위 폴더가 포함되지 않아 `from backend.iot...` 형태의 절대 경로 임포트가 실패함.
  ```
  ModuleNotFoundError: No module named 'backend'
  ```

### 💡 개선 요청
1. **`backend/main.py` 최상단에 `sys.path` 자동 추가 코드 기본 탑재**:
   ```python
   import os, sys
   CURRENT_DIR = os.path.dirname(os.path.abspath(__file__))
   PARENT_DIR = os.path.dirname(CURRENT_DIR)
   if CURRENT_DIR not in sys.path:
       sys.path.insert(0, CURRENT_DIR)
   if PARENT_DIR not in sys.path:
       sys.path.insert(0, PARENT_DIR)
   ```
2. 또는 `backend` 내부 모듈 간 임포트를 상대 경로(`from .iot...` 또는 `from iot...`)로 일관되게 작성하도록 규칙 가이드 제공.

---

## 3. Windows PowerShell `pip freeze` 시 UTF-16 LE 인코딩 생성 이슈

### 🔴 문제 현상
- Windows PowerShell에서 `pip freeze > requirements.txt` 파이프라인 리다이렉션을 사용할 경우 기본 인코딩이 **UTF-16 LE**로 저장되어, 다른 도구나 파서에서 읽을 때 지원되지 않는 MIME 타입 에러(`unsupported mime type text/plain; charset=utf-16le`)가 발생함.

### 💡 개선 요청
- 매뉴얼이나 가이드 스크립트에 UTF-8 명시 명령어로 표기:
  ```powershell
  pip freeze | Out-File -Encoding utf8 requirements.txt
  ```

---

## 4. Next.js 대시보드 SSR Hydration Mismatch (시간 표시)

### 🔴 문제 현상
- `SensorCard`, `ActuatorCard`, 헤더의 마지막 갱신 시각에 `new Date().toLocaleTimeString()`을 직접 렌더링할 경우, **서버 렌더링(SSR) 시점의 시간**과 **브라우저 클라이언트 마운트 시점의 시간**이 달라 Next.js Hydration Mismatch 오류가 발생함.
  ```
  Uncaught Error: Hydration failed because the server rendered text didn't match the client.
  ```

### 💡 개선 요청
1. `dashboard-ui-design` 스킬 및 컴포넌트 템플릿에 시간 렌더링 태그마다 `suppressHydrationWarning` 속성을 기본 포함.
2. 초기 렌더링 시 빈 문자열 또는 `동기화 중...`으로 표시하고 클라이언트 마운트(`useEffect`) 후 시간을 갱신하는 패턴 권장.

---

## 5. 백엔드 필수 의존성에 `httpx` 누락

### 🔴 문제 현상
- FastAPI의 `TestClient` 및 비동기 엔드포인트 통합 테스트 시 최신 Starlette/FastAPI 버전에서는 `httpx`가 필수 패키지로 요구됨.
  ```
  RuntimeError: The starlette.testclient module requires the httpx package to be installed.
  ```

### 💡 개선 요청
- `backend/requirements.txt` 및 0단계 설치 목록에 `httpx`를 기본 포함하도록 가이드 수정:
  ```
  fastapi uvicorn[standard] websockets pymysql python-dotenv httpx
  ```

---

## 6. 멀티 프로세스 실행 가이드 명확화 (백엔드 오프라인 시 혼선 방지)

### 🔴 문제 현상
- 프론트엔드(`npm run dev`)만 실행한 상태에서 브라우저 접속 시 백엔드(`http://localhost:8000`)가 꺼져 있어 `Control request failed: TypeError: Failed to fetch` 콘솔 에러가 다수 발생함.

### 💡 개선 요청
- 매뉴얼 2단계 및 3단계에 **동시에 열어두어야 하는 터미널 탭 목록(최소 3개)**을 시각적 다이어그램이나 체크리스트로 명시:
  - **터미널 1**: 백엔드 (`uvicorn main:app --reload --port 8000`)
  - **터미널 2**: 프론트엔드 (`npm run dev`)
  - **터미널 3**: 비전 클라이언트 (`python vision/main.py`)
- 프론트엔드 훅(`useSmartDeviceSocket.ts`)에서 백엔드 미실행 시 불필요한 `console.warn`을 줄이고 대시보드 상단 배지에 "연결 대기 중"으로만 깔끔하게 표시되도록 템플릿 개선.

---

## 7. 구글스페이스 무료 티어 토큰 절약을 위한 '학생 수작업(Manual) 전환' 가이드

마이스터고 정보통신과 1학년 학생들은 파이썬, C언어, 컴퓨터활용능력 등의 기초 지식과 실습 경험을 보유하고 있습니다. 교사의 지도 하에 **환경 설정, 패키지 설치, DB 스크립트 실행, 서버 구동 등의 수작업을 직접 수행**하도록 하면, 전체 IoT 시스템의 구조(프론트-백-DB-영상인식)를 체계적으로 학습할 수 있을 뿐만 아니라 **Gemini API 무료 티어 토큰을 80% 이상 절감**할 수 있습니다.

따라서 AI는 단순 파일 생성이나 CLI 실행 같은 보일러플레이트 작업 대신, **학생이 기획한 팀별 맞춤형 비즈니스 로직(센서/액추에이터 제어 및 트리거 로직)** 구현에만 집중하도록 역할을 명확히 분리해야 합니다.

### 📋 토큰 80% 절감을 위한 단계별 수작업 전환 권장 항목

| 단계 | 학생 수작업 (직접 수행 — 토큰 0 소모) | AI에게 요청할 작업 (최소 프롬프트) | 절감 효과 |
|---|---|---|---|
| **0단계 (초기 구축)** | • `python -m venv venv`<br>• `pip install ...`<br>• `npm install ...`<br>• `copy .env.example .env`<br>• `npx create-next-app` 직접 실행 | • 빈 폴더 생성이나 패키지 설치는 AI에게 시키지 않음 | 초기 대형 컨텍스트(수만 토큰) 절약 |
| **1단계 (기획/PRD)** | • 브라우저에서 **무료 ChatGPT/Claude 웹**을 사용하여 PRD 작성 및 `AGENTS.md` 완성 후 복사-붙여넣기 | • Gemini API를 통한 문서/기획서 생성 일체 생략 | 기획 단계 Gemini 토큰 100% 절감 (0 토큰 소모) |
| **2단계 (DB & Mock)** | • XAMPP 실행 및 phpMyAdmin에서 DB 생성<br>• `Get-Content db\init.sql \| mysql -u root` 직접 실행 | • `AGENTS.md`의 디바이스 목록에 맞춘 `init.sql` 시드 데이터와 `MockDeviceProvider` 생성만 요청 | DB 생성/서버 구동 등 단순 실행에 토큰 소모 방지 |
| **대시보드 UI** | • `lucide-react` 패키지 설치 (`npm install lucide-react`)<br>• `npm run dev` 서버 실행 및 브라우저 확인 | • `components/dashboard` 카드 레이아웃 및 훅 생성만 요청 | 패키지 설치 및 뷰어 렌더링 토큰 절약 |
| **3단계 (영상인식)** | • PyTorch 및 OpenCV, YOLO 패키지 설치 (`pip install torch ...`)<br>• 카메라 인덱스 설정 (`.env` 수정) | • `AGENTS.md`의 감지 대상을 필터링하는 `vision/main.py` 파이프라인 생성만 요청 | 대용량 설치 로그의 토큰 소모 방지 |

---

### 💡 플러그인 / 스타터 킷 아키텍처 개선 제안 (사전 보일러플레이트 탑재)

AI가 매번 기본 코드를 작성하느라 토큰을 소모하지 않도록, **변하지 않는 공통 유틸리티/인터페이스 코드를 스타터 킷에 미리 파일로 탑재**할 것을 강력히 제안합니다:

1. **사전 탑재 권장 파일**:
   - `backend/iot/base.py` (DeviceProvider 추상 클래스)
   - `backend/websocket_manager.py` (WebSocket 브로드캐스트 매니저)
   - `frontend/components/dashboard/statusColor.ts` (상태 색상 유틸)
   - `frontend/components/dashboard/ConnectionBadge.tsx` (연결 배지)
2. **효과**:
   - AI는 전체 구조를 다시 생성할 필요 없이, **"우리 팀 디바이스 목록과 트리거 규칙에 맞는 Provider / Router만 수정/추가"**하게 되므로 프롬프트 및 응답 토큰 소모량이 **기존 대비 70~80% 감소**합니다.

---

### ⚙️ Antigravity Rules "Always On" 최소화 가이드

- `.agents/rules/`에 존재하는 10개의 규칙을 전부 `Always On`으로 설정하면, **매 채팅 프롬프트마다 8,000 ~ 12,000 토큰이 고정 소모**되어 몇 번의 대화만으로 무료 티어 한도에 도달합니다.
- 반드시 아래 **3개 핵심 규칙만 Always On**으로 유지하도록 매뉴얼에 명시해야 합니다:
  1. `karpathy-principles` (작업 원칙)
  2. `security-rules` (보안 및 시크릿 방지)
  3. `coding-standards` (코드 품질)
- 나머지 규칙(`api-rules`, `db-rules`, `hardware-rules`, `vision-rules` 등)은 해당 작업을 요청할 때만 프롬프트에 `db-agent`, `vision-agent` 등의 역할을 명시하여 필요할 때만 호출하도록 유도합니다.

---

## 8. 실습실 네트워크 및 웹캠 점유 충돌 방지 안내

### 🔴 문제 현상
1. **YOLO 모델 다운로드 지연 및 방화벽 차단**: `vision/main.py` 최초 실행 시 `yolov8n.pt`(약 6.2MB)를 GitHub에서 다운로드하는데, 학교 실습실의 30~40명 학생이 동시에 실행하면 네트워크 병목이 발생하거나 학내망 보안 방화벽에 의해 다운로드가 차단될 수 있음.
2. **웹캠 프로세스 독점 충돌**: Windows는 하나의 웹캠을 한 번에 한 프로그램만 사용할 수 있어, Zoom, Teams, 또는 웹 브라우저 카메라가 켜져 있으면 `cv2.VideoCapture`가 화면을 읽지 못하고 검은 화면이나 무한 대기 상태가 됨.

### 💡 개선 요청
1. **`yolov8n.pt` 모델 사전 포함**: 스타터 킷 배포 시 `vision/yolov8n.pt`를 미리 다운로드해 폴더 안에 포함시켜 배포 (네트워크 병목 및 차단 원천 해결).
2. **사전 준비 체크리스트 매뉴얼 강조**: 3단계 시작 전 "웹캠을 사용할 수 있는 다른 앱(카메라 앱, 브라우저 등)을 모두 종료하세요" 안내를 매뉴얼 상단에 눈에 띄게 강조.


